development:
  adapter: async
  adapter: redis
  url: redis://localhost:3000

test:
  adapter: async

production:
  adapter: redis
  url: redis://localhost:6379/1
  channel_prefix: afterglow_production
